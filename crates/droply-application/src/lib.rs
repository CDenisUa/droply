//! Use-case orchestration and trait boundaries (`MediaSourceAnalyzer`,
//! `DownloadStrategy`, `MediaProcessor`, `JobQueue`, `UrlValidator`, ...).
//!
//! Intentionally empty in Phase 0 (skeleton) — populated starting Phase 1
//! (direct file downloader), see `docs/CURRENT_STATE.md` and AGENTS.md §15
//! ("don't create a trait until there's a concrete near-term use for it").
